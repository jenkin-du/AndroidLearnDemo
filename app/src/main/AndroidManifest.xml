<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.uestc.jenkin.androidlearndemo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".AnimListActivity" />
        <activity android:name=".SecondActivity" />
        <activity android:name=".DrawViewActivity" />
        <activity android:name=".PorterDuffXfermodeActivity" />
        <activity android:name=".SurfaceViewActivity" />
        <activity android:name=".MenuAnimActivity" />
        <activity android:name=".BlurActivity" />
        <activity android:name=".ClipActivity" />
        <activity android:name=".TransitionActivity" />
        <activity android:name=".Transition2Activity" />
        <activity android:name=".RippleActivity" />
        <activity android:name=".CircularRevealActivity" />
    </application>

</manifest>